<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyundai.minihompy.dao.BoardDAO">

	<insert id="insert" parameterType="BoardDTO">
		INSERT INTO board(bno, id, name, title, content, regdate, readcount, password)
		VALUES(sq_board.nextval, #{id}, #{name}, #{title}, #{content}, sysdate, 0, #{password} )
	</insert>

	<select id="getList" resultType="BoardDTO">
		SELECT bno, title, name, regdate, readcount
		FROM board
		ORDER BY bno DESC
	</select>

	<select id="getDetail" parameterType="long"
		resultType="BoardDTO">
		SELECT bno, title, name, regdate, readcount, content
		FROM board
		WHERE bno=#{bno}
	</select>

	<delete id="delete" parameterType="long">
		DELETE board
		WHERE bno=#{bno}
	</delete>

	<update id="update" parameterType="BoardDTO">
		UPDATE board
		SET 
		name = #{name},
		title = #{title},
		content=#{content}
		WHERE bno=#{bno}
	</update>



</mapper>
